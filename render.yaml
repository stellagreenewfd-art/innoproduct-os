services:
  - type: web
    name: innoproduct-os
    env: node
    buildCommand: npm install
    startCommand: node server.js
    disk:
      name: data
      mountPath: /opt/render/project/src/data
      sizeGB: 1
